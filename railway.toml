[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Railway injeta $PORT automaticamente — o Dockerfile já usa ${PORT:-8080}
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
